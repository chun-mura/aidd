# 正解キー: cases/structure.md (週次利用レポート自動生成機能)

このファイルは /aidd:eval の採点専用。**design-review の実行が完了するまで読んではならない** (reviewer への汚染防止)。

## シード欠陥

コアシード (バージョン間比較の分母): **S1, S2**。S3-S6 は昇格シードで拡張検出率のみに数える。

| ID | 該当セクション | 対応観点 | 期待深刻度 | 欠陥 | 一致判定基準 |
|---|---|---|---|---|---|
| S1 | 構成と責務 | 責務分割 | mid | `ReportManager` が収集・集計・整形・送信・スケジュール管理の5責務を1クラスに集約している | `ReportManager` の責務過多・分割不足 (God class 相当) を指摘していれば検出。分割の切り口が異なっても可 |
| S2 | インターフェース | インターフェース | mid | `generate()` が生レコード・jinja2 環境・接続済みSMTPクライアントという内部実装の道具立てを呼び出し側に要求しており、内部を知らないと使えない | `generate()` の引数が内部実装を漏らしている・呼び出し側の負担が過大である旨を指摘していれば検出 |
| S3 | エラー処理 | エラー処理 | high | `smtplib.SMTP.sendmail()` は一部宛先拒否を例外でなく戻り値で返すため、「失敗したら例外を上げて中断」は全滅ケースのみカバーし、一部配信失敗が無通知になる | 部分的な送信失敗が検知されない旨を指摘していれば検出 (smtplib の仕様への言及は必須としない。「宛先単位の失敗確認がない」でも可) |
| S4 | 構成と責務 / 運用 | 構造 or データフロー or YAGNI | mid | スケジュール管理の主体が report.yaml (送信スケジュール設定)・`ReportManager` (次回実行時刻の管理)・外部 cron ジョブの3箇所に分散し、真実の情報源が未確定 | スケジュール管理の重複・持ち主の不明確さを指摘していれば検出 (2箇所の指摘でも可) |
| S5 | 構成と責務 vs インターフェース | 構造 | mid | 責務表は `ReportManager` が「収集クエリ発行」を担当とするが、`generate()` は `raw_rows` を呼び出し側が SELECT して渡す前提で、文書内で自己矛盾 | 収集クエリの主体が矛盾している旨を指摘していれば検出 |
| S6 | 運用 | 運用 | mid | 3種類の失敗経路が定義されているのに、運用節のログ設計は exit code と成功時 INFO のみで、どの経路で失敗したかを判別できない | 失敗種別を判別するログ・観測手段の欠如を指摘していれば検出 |

(S3-S6 は 2026-07-18 の初回評価でキー外指摘として検出され、ユーザー承認により昇格。ケース作成時の意図的シードは S1-S2 のみ)

## デコイ (指摘したら誤検出)

| ID | 該当セクション | 内容 | 判定 |
|---|---|---|---|
| D1 | インターフェース (未定記載) → 決定事項 | キャッシュ保存先の「未定」は決定事項節 (2026-07-01) で「キャッシュしない」と解決済み | 「キャッシュ保存先が未定・未解決」と指摘した場合は誤検出としてカウントする (読了プロトコル違反の検証)。決定済みである旨に触れた上での別論点の指摘は誤検出としない |
